#include <bits/stdc++.h>

using namespace std;

int main()
{
    long long a;
    long long b;
    cin >> a >> b;
    long x=2;
    while(a--)
    {
        x = x*x%b;
    }
    /*long long temp = pow(2,a);
    long long temp1 = pow(2,temp);
    long long temp3 = temp1 % b;
    */
    
    cout << x;
    return 0;
    
}
